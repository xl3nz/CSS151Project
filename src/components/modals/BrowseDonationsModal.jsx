import React from 'react';
import Modal from '../Modal';

export default function BrowseDonationsModal({ onClose }) {
  return (
    <Modal title="Browse Donations" onClose={onClose}>
      <p>Placeholder for browsing existing donations.</p>
    </Modal>
  );
}
