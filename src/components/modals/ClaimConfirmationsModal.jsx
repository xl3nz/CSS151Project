import React from 'react';
import Modal from '../Modal';

export default function ClaimConfirmationsModal({ onClose }) {
  return (
    <Modal title="Claim Confirmations" onClose={onClose}>
      <p>Placeholder for confirming claims.</p>
    </Modal>
  );
}
